<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
     <!-- <record id="res_partner_inherit_list_view" model="ir.ui.view">
        <field name="name">res.partner.inherit.view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='display_name']" position="after">
                <field name="seat_capacity" string="Seat Capacity" invisible="company_type!='company'"/>
            </xpath>

        </field>
    </record> -->
    <record id="res_partner_inherit_form_view" model="ir.ui.view">
        <field name="name">res.partner.inherit.form.view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="before">
                <field name="seat_capacity" string="Seat Capacity" invisible="company_type!='company'"/>
            </xpath>

        </field>
    </record>
</odoo>